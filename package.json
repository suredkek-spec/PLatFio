{
  "name": "platfio-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/web",
    "apps/backend"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @platfio/web",
    "dev:backend": "npm run dev --workspace @platfio/backend",
    "build:web": "npm run build --workspace @platfio/web",
    "build": "npm run build --workspace @platfio/web && npm run build --workspace @platfio/backend",
    "lint": "npm run lint --workspace @platfio/web && npm run lint --workspace @platfio/backend"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
